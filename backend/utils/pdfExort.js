// Simple placeholder export-PDF handler to avoid runtime error.
// Replace with actual PDF generation if needed later.

const exportPdf = (req, res) => {
  res.status(200).json({ message: 'PDF export endpoint placeholder' });
};

export default exportPdf;